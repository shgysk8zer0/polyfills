repos:
- repo: https://github.com/gitleaks/gitleaks
  rev: v8.16.3
  hooks:
  - id: gitleaks
- repo: local
  hooks:
  - id: local-eslint
    name: Run ESLint
    entry: npm run lint:js --if-present
    language: system
    types_or: [javascript, jsx, ts, tsx]
